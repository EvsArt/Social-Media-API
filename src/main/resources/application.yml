constantsFiles:
    strings: 'string_constants.yml'

image:
    path: '/images'

spring:
    datasource:
        driver-class-name: org.postgresql.Driver
        url: 'jdbc:postgresql:socialmediaapi'
        username: postgres
        password: postgres
    jpa:
        hibernate:
            ddl-auto: create